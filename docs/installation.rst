============
Installation
============

At the command line::

    $ easy_install booksforcha

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv booksforcha
    $ pip install booksforcha
